<div class="container  mt-3 mb-5">
  <div class="row">
    
    <%= render 'users/user_info', book: @book %>
    
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <hr noshade>
      <table>
        <td width=>
          <div class="user-info">
            <%= image_tag @book.get_image %><br>
            <%= link_to user_path(@book.user_id) do %>
              <%= @book.user.name %>
            <% end %>
          </div>
        </td>
        <td>
          <%= link_to book_path(@book.id) do %>
            <%= @book.title %>
          <% end %>
        </td>
        <td>
          <%= @book.body %>
        </td>
        <td>
          <% if @book.user == current_user %>
            <%= link_to "Edit", edit_book_path(@book.id), class: 'btn btn-default' %>
          <% end %>
        </td>
        <td>
          <% if @book.user == current_user %>
            <%= link_to "Destroy", books_path, method: :delete, "data-confirm" => "本当に消しますか?", class:'btn btn-default' %>
          <% end %>
        </td>
      </table>
    </div>
  </div>
</div>